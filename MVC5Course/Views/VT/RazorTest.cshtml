@model int[]

@for (int i = 0; i < Model.Length; i++)
{
    @Model[i]
}

@for (int i = 0; i < Model.Length; i++)
{
<text>@(Model[i]) </text>}

<ul>
@for (int i = 0; i < Model.Length; i++)
{
  <li>@(Model[i])</li>
}
</ul>